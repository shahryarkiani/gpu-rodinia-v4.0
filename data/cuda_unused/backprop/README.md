# Backprop Data Directory

## Overview

The Backpropagation benchmark generates its own synthetic data internally and does not require external data files.

## Usage

Refer to the implementation-specific README files in the CUDA, OpenCL, or OpenMP directories for build and run instructions.